version: "2"
sql:
  - engine: "postgresql"
    queries: "queries/"
    schema: "migrations/"
    gen:
      go:
        package: "db"
        out: "packages/db"
        sql_package: "database/sql"
        emit_json_tags: true
        emit_db_tags: true
        emit_pointers_for_null_types : false
